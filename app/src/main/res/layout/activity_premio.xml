<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="cacean.sorteos.PremioActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ticket:"
        android:id="@+id/tvwTicket"
        android:textColor="@android:color/black"
        android:textSize="@dimen/abc_dropdownitem_icon_width"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/txtTicket"
        android:inputType="number"
        android:layout_below="@+id/tvwTicket"
        android:layout_alignParentStart="true"
        android:hint="Digite el sorteo + el consecutivo a 4 posiciones" />

    <Button
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:text="Escanear Ticket"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:id="@+id/btnScan"
        android:layout_below="@+id/txtTicket"
        android:layout_alignParentStart="true"
        android:background="@android:drawable/btn_default" />

    <Button
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:text="Verifica Ticket"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:id="@+id/btnVerif"
        android:layout_alignTop="@+id/btnScan"
        android:layout_toEndOf="@+id/btnScan"
        android:background="@android:drawable/btn_default" />

</RelativeLayout>
